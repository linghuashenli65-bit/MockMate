# 更新日志

## [2026-08-02] — 岗位画像支持目标公司定向搜索

### ✨ 新功能
- **公司定向招聘查询**：`/api/research` 新增 `company` 字段，指定目标公司时追加公司维度搜索（招聘 JD / 薪资待遇 / 官网招聘 / 面经），画像输出「招聘状态 / 薪酬范围 / 公司洞察 / 参考来源」
- **来源提取**：搜索解析增加结果链接提取，AI 输出最多 5 条参考来源
- **缓存按「岗位@公司」隔离**：不同公司的同岗位画像互不污染
- 前端岗位画像卡片展示公司徽标、招聘状态、薪酬、公司洞察与参考来源；未查到公司专属信息时如实降级为市场概况
- **岗位同义词扩展**：搜索前由 AI 生成同义岗位名称（如「AI应用开发工程师」→「大模型应用开发工程师/人工智能工程师」），失败时规则兜底；同义词加入搜索词与画像提示词，扩大招聘 JD / 面经召回

### 🐛 Bug 修复
- **设置页提供商切换失效**：下拉框缺少 v-model 绑定，切换时提交的是旧值；改为读取用户实际选中的选项，切换后下拉、提示与后端 `/api/settings` 状态保持一致
- **ASR WebSocket 鉴权回归**：HTTPBearer 依赖不兼容 WebSocket，改为 JWT token 查询参数解码用户设置中的 Qwen Key，恢复语音输入
- **岗位画像大提示词空返回**：搜索摘要拼接过大导致推理模型 token 耗尽、返回空内容，前端只显示通用兜底卡片；改为搜索摘要去重截断、提示词限长、明确要求直接输出 JSON，失败时用紧凑提示词自动重试一次，并注入目标公司字段
- **DeepSeek 模型名更新**：`deepseek-chat` 已不存在，笔试判卷模型统一改为 `deepseek-v4-flash`

---

## [2026-08-01] — Vue 3 前端迁移 + 拟真历史独立 + 设置重构

### ✨ 新功能
- **前端全面迁移到 Vue 3 + Vite**：9 个页面 + 登录页全部组件化（Pinia 状态、Vue Router 路由、Chart.js/marked npm 包引入），后端自动托管构建产物并支持 SPA 回退
- **设置页重构 + 服务端 Key 存储**：API Key 加密保存在用户账号下（Fernet 加密、接口只回显掩码），换设备自动同步；能力模型映射支持留空使用默认
- **拟真面试历史独立成页**：拟真面试页内新增「开始面试 / 历史记录」子导航，详情按拟真维度重新设计（综合评分、考察覆盖、面试官均分、逐题评分问答）
- **模拟面试历史并入模拟面试页**：同样通过子导航访问，导航精简为 7 个 Tab（移除顶层历史记录与语音识别页）
- **评分持久化**：拟真面试结束（REST/WebSocket 两条路径）统一把逐题评分与问答写入数据库，历史详情可完整回看

### 🐛 Bug 修复
- **历史记录图表空白**：修复图表容器高度塌陷 + 加载时序错误（loading 状态阻塞 canvas 挂载）
- **设置页内容空白**：修复裸 `<template>` 包裹层导致的内容不渲染
- **语音播报开关**：修复选中态轨道透明（`--accent1` 未定义）与开关宽度被撑大问题
- **历史页健壮性**：薄弱点/建议字段非数组时不再导致整页崩溃

### 🔧 优化
- 历史记录 / 自定义题目 / 微调列表统一每页 10 条分页
- 最近趋势数值与其他统计卡片字号对齐（24px）
- 常见薄弱点改为胶囊标签展示（次数 >1 才显示角标）
- 导航栏收紧：7 个 Tab，消除拥挤换行

---

## [Unreleased] — 后续计划

### 预告
- **LoRA 微调训练器** — 基于采集的训练数据和用户反馈评分，对 AI 评分模型进行 LoRA 微调，提升评分准确度
- **`trainer.py`** — 使用 PEFT + Transformers + TRL 实现 Qwen2.5-3B-Instruct 微调
- **微调前后对比** — 基于记录的 latency/token 数据对比微调前后的性能变化

---

## [2026-05-22] — 拟真面试全面升级

### ✨ 新功能
- **多层安全防御体系**：5 层安全架构 — InputGuard（输入注入检测）、Prompt Isolation（数据隔离）、OutputGuard（关键词泄漏扫描）、StateVerifier（状态机校验）、MemoryGuard（记忆污染防护），覆盖 8 种攻击类型
- **HTTPS 支持**：自动生成自签名 SSL 证书，局域网其他设备可通过 HTTPS 访问并使用麦克风
- **语音输入（ASR）**：录音完成后自动调用语音识别接口转文字，填入回答输入框
- **面试官人设增强**：aggressiveness、follow_up_depth、interruption_rate 等行为属性注入 prompt，不同面试官问出不同风格的问题

### 🐛 Bug 修复
- **麦克风不可用**：`navigator.mediaDevices` 在非安全上下文中为 undefined，添加 `_checkMediaDevices()` 检测并提示用户改用 localhost/HTTPS
- **面试官切换次序错乱**：`audio_chunk` 在切换动画之前到达，导致旧卡片亮呼吸灯、音频先播。新增 `switch_interviewer` WebSocket 消息在音频流之前发送，确保先切卡片再播音频

### 🔧 优化
- **追问权重提升**：`follow_up_bonus` 从 0.3 提升至 0.5，衰减速度减半，高追问深度面试官额外加成；短回答（<80字）自动加权重触发追问
- **阶段推进质量驱动**：不再使用固定题数阈值，改为综合评分（≥75分）、回答长度（≥150字）、时间压力（>70%）、面试官追问深度等多维信号动态决策
- **问题连续性增强**：追问和切换场景使用不同的 prompt 指令，追问时强制要求紧扣上一条回答、引用候选人说过的话
- **历史上下文完整展示**：最后一条回答完整展示（最多500字），之前的回答截断到200字，不再统一截断150字
- **ASR 集成到主录音流程**：之前仅语音测试按钮支持 ASR，主录音按钮只是占位提示，现在两者都支持语音转文字

---

## [2026-05-10] — 通义千问支持 + 题库预生成 + TTS 优化

### ✨ 新功能
- **通义千问 (Qwen) 作为第三 AI 提供商**：支持推理出题、对话、图片识别（qwen-vl-plus）、语音合成（CosyVoice），自动 fallback 到 MiMo/DeepSeek
- **千问模型可配置**：设置页支持分别配置推理模型、对话模型、判卷模型、TTS 模型，留空使用默认值（qwen-plus / qwen-turbo / cosyvoice-v1）
- **预生成题库 (QuestionPool)**：面试时后台并行预生成 easy/medium/hard 三类题目到池中，出题从池中 pop（毫秒级），消除 30s 串行出题等待；严格上限（总题数+4），盈余缓存复用
- **TTS 实时开关**：面试标题栏增加 🔊/🔇 按钮，可随时切换语音播报，关闭时折叠为"播放本题语音"按钮
- **TTS 提供商自适应**：切换到 DeepSeek 时自动禁用语音开关并提示"不支持语音"
- **千问模型请求头传递**：浏览器 localStorage 中的模型配置通过 HTTP 请求头发送到后端，无需后端重启

### 🐛 Bug 修复
- **状态指示器只认 MiMo**：左上角状态灯只检查 mimo_ready/deepseek_ready，忽略 qwen_ready，导致千问可用时仍显示黄色"未完全配置"
- **简历评分 JSON 换行符**：AI 输出 JSON 字符串内的真实换行符导致 json.loads 报错，添加 `_repair_json_newlines` 修复

### 🔧 优化
- **语音播报优化**：mid-interview 实时开关（通过 enable_tts 参数同步后端）、面试页 TTS 指示器、设置页显示当前语音提供商
- **千问模型默认值**：config.py 新增 QWEN_MODEL_REASONER / QWEN_MODEL_CHAT / QWEN_MODEL_WRITTEN_EVAL / QWEN_MODEL_TTS 可配置
- **状态指示器**：改为按前端选中提供商判断，更准确反映用户实际配置状态
- **文档同步更新**：README.md 架构图新增 Qwen 客户端和 finetune 模块，技术栈表补充千问相关能力，项目结构同步新增 10 个 JS 模块；frontend/README.md 全面跟进 7 Tab 布局和新增模块

---

## [2026-05-08] — 训练数据采集 + 反馈系统
- **训练数据采集模块** (`finetune/`)：自动记录每次 AI 评分结果，包括题目、回答、评分、延迟和 token 用量
- **点赞/点踩反馈系统**：评分结果上的 👍/👎 按钮，点踩时可提交修正内容，修正数据存入 `reviewed` 目录
- **训练数据管理页面**：前端新增"微调"Tab，支持数据统计浏览、按类型和质量过滤
- **AI 客户端延迟和 Token 跟踪**：`AIClient`、`MiMoClient`、`DeepSeekClient` 新增 `last_latency_ms` 和 `last_usage` 属性，记录每次调用的性能和 token 消耗
- **Markdown 渲染支持**：`M.md()` 函数基于 marked 库渲染 GFM Markdown，历史记录、研究报告等全面改用 markdown 显示

### 🐛 Bug 修复
- **评估 JSON 解析失败**：修复 `_EVAL_JSON_TEMPLATE` 双花括号 `{{...}}` 导致的 JSON 解析失败
- **评语截断**：`max_tokens=1024` 过小导致长评语被截断，调整为 2048
- **面试结束按钮状态**：结束后首页"开始模拟面试"按钮仍显示"正在准备面试..."，`cleanup()` 新增按钮状态恢复
- **JSON 换行符**：AI 输出 JSON 字符串内的真实换行符导致 `json.loads` 报错，添加 `_repair_json_newlines` 修复函数
- **MySQL VALUES 弃用告警**：`auth.py` 添加 `warnings` 过滤抑制 `VALUES function is deprecated` 告警
- **出题 JSON 格式**：AI 返回含真实换行符的无效 JSON 导致出题显示 `"question"` 字段名，`_parse_question` 新增 JSON 预处理

### 🔧 优化
- **备选笔试题库大幅扩充**：各岗位（通用/后端/前端/AI/运维/测试）新增约 60 题，总计超 150 题，确保 20 题笔试无重复
- **TTS 输出格式**：从 `.mp3` 改为 `.wav` 格式
- **评估提示词优化**：添加简洁度约束，输出控制在 800 token 内
- **模型配置扩展**：`config.py` 新增 `DEEPSEEK_MODEL_CHAT`、`DEEPSEEK_MODEL_WRITTEN_EVAL` 等模型选择变量

---

## [2026-05-06] — 面试轮次提示词优化

### ✨ 新功能
- 面试轮次提示词大幅优化：技术一面、技术二面、综合面有明确的区分度和独立的考察方向
- 项目页面添加界面预览图片

---

## [2026-05-05] — 面试体验全面优化

### ✨ 新功能
- **倒计时系统**：每题限时，时间到自动提交
- **断点续面**：刷新/关闭页面后恢复未完成的面试
- **跳过题目**：可跳过暂时不会的题目，回头再答
- **解题提示**：AI 生成解题思路提示
- **语音问答**：支持语音输入和播报
- **题目收藏**：收藏好题到自定义题库
- **自定义题目**：支持自定义题目列表
- **数据分析**：面试历史统计图表
- **PDF 导出**：报告打印/导出
- **岗位画像研究**：AI 搜索岗位要求，生成结构化画像
- **用户系统**：多用户支持、邮箱验证码登录

### 📚 文档
- 更新 README 文档，补充新功能说明和 API 文档

---

## [2026-05-01] — 笔试功能完善

### ✨ 新功能
- **笔试备选题按岗位分类匹配**：根据岗位关键词（后端/前端/AI/运维/测试）自动匹配最相关的题库

### 🐛 Bug 修复
- **笔试判卷**：改为直接比对 `correct_answer`，不再调 AI 判卷，提高准确率和速度
- **correct_answer 误判**：修复 AI 出题时 `correct_answer` 错位导致的判卷错误

### 🔧 优化
- 前端模块化重构

---

## [2026-04-29] — 初始版本

### ✨ 功能
- **AI 面试模拟**：输入简历和目标岗位，AI 面试官出题并评分
- **多轮次面试**：技术一面、技术二面、综合面、笔试
- **面试报告**：AI 生成面试总结报告（评分、优点、改进建议）
- **简历评分**：AI 对简历进行评分并提供优化建议
- **双 AI 后端**：支持 MiMo 和 DeepSeek，自动路由和 fallback
- **离线部署**：支持局域网内多设备访问

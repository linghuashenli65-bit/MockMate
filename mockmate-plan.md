# MockMate - AI 面试模拟陪练 执行计划 v2

## 新增需求
1. **适配 DeepSeek API** — 同时支持 MiMo 和 DeepSeek，用户可选
2. **网络信息搜集** — 出题前搜索真实岗位要求和面经，让题目有针对性

## 整体架构

```
┌──────────────────────────────────────────────────┐
│                  前端 (HTML+JS)                   │
│  简历上传 | 面试界面 | 录音 | 报告 | API选择      │
└────────────────────┬─────────────────────────────┘
                     │ HTTP / WebSocket
┌────────────────────▼─────────────────────────────┐
│              FastAPI 后端                         │
│                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────┐  │
│  │ 简历解析  │ │ 面试引擎  │ │ 网络信息搜集      │  │
│  │ (多模态)  │ │ (出题+评  │ │ 岗位要求+面经爬取  │  │
│  │           │ │  估+追问) │ │  → 提炼关键词     │  │
│  └──────────┘ └────┬─────┘ └────────┬─────────┘  │
│                     │                │              │
│                     ▼                ▼              │
│  ┌────────────────────────────────────────────┐   │
│  │          AI Client 统一接口                  │   │
│  │  用户选择: MiMo API / DeepSeek API           │   │
│  │  ┌────────────┐  ┌──────────────┐          │   │
│  │  │ MiMoClient │  │ DeepSeekCli  │          │   │
│  │  │ 推理+多模态 │  │  推理+       │          │   │
│  │  │ +TTS 语音  │  │  (文本)      │          │   │
│  │  └────────────┘  └──────────────┘          │   │
│  └────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────┘
```

## API 适配策略

| 能力 | MiMo | DeepSeek | 说明 |
|------|------|----------|------|
| 推理/出题/评估 | ✅ 推理模型 | ✅ 推理模型 | 两者均可 |
| 多模态(简历OCR) | ✅ 多模态模型 | ❌ | 仅 MiMo 支持 |
| 语音合成(TTS) | ✅ 语音合成 | ❌ | 仅 MiMo 支持 |
| 网络搜索增强 | ✅ 可集成 | ✅ 可集成 | 独立的搜索模块 |

**策略**：用户选择一个主要 Provider，系统自动 fallback：
- 选 DeepSeek 时，简历解析和 TTS 仍走 MiMo
- 选 MiMo 时，全部走 MiMo

## 网络信息搜集模块

```
用户输入目标岗位 (如 "Python后端开发 3年经验")
                │
                ▼
  搜索招聘网站 → 提取岗位要求(技能/职责/年限)
  搜索面经社区 → 提取常见面试题和考察重点
                │
                ▼
  整合成"岗位画像" → 传给面试引擎 → 生成针对性题目
```

数据来源策略：
- 使用 `httpx` + 简易爬虫抓取公开信息
- 优先使用 DuckDuckGo/Bing 搜索（不需要 API Key）
- 如果搜索失败，使用 AI 模型自身的知识作为备选

## 分步实现计划

### Phase 1: 项目骨架 + API 双适配
- 目录结构、依赖安装
- FastAPI 基础服务
- MiMoClient（复用 FileBrain）
- DeepSeekClient（新写）
- UnifiedAIClient（统一接口，自动路由）

### Phase 2: 网络信息搜集模块
- WebSearchEngine（搜索岗位要求）
- InterviewKnowledgeBase（面经 + 技能图谱提取）
- 岗位画像生成（结构化输出）

### Phase 3: 简历解析
- PDF/图片上传 → 解析
- 多模态提取简历内容（MiMo）
- DeepSeek 纯文本解析（备选）

### Phase 4: 面试引擎
- 出题系统（结合岗位画像+简历）
- 回答评估（多维度评分）
- 追问逻辑（动态决策）

### Phase 5: 语音功能
- 浏览器录音
- MiMo TTS（也可选其他 TTS）
- 对话流程控制

### Phase 6: 前端 + 报告
- 面试界面 UI
- 实时反馈
- 报告生成与历史

## 依赖

```bash
pip install fastapi uvicorn aiofiles httpx python-multipart beautifulsoup4 lxml
```

beautifulsoup4 + lxml 用于解析网页内容。
语音录制使用浏览器原生 API。
